import { ReplaceSymbolWithAnotherSymbolPipe } from './replace-symbol-with-another-symbol.pipe';

describe('ReplaceSymbolWithAnotherSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSymbolWithAnotherSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
